# 版本管理

品牌名固定为 **MC Clicker 2.0**；发行版本采用语义化版本，独立于存档格式。

- 唯一版本来源：本目录 `package.json.version`。`src/release.js` 读取该值；Vite 将同一版本注入 HTML 标题、描述和启动画面，设置页亦读取同一值。
- 修复版：递增补丁号，如 `1.2.2`。功能重构：递增次版本，如 `1.3.0`。未完成的开发版使用 `1.3.0-dev.1`，不得标记为已发布。
- 存档格式由 `src/game.js` 单独维护，目前为 7。V1.3 将网页装扮、环境、场景装扮分开持久化，兼容读取 2–6 版旧档。只改页面版本不增加存档版本，更不清空旧档。
- 原 V1.2 装扮与世界氛围方案移入 V1.3 排期；V1.2.1、V1.2.2 是已经发生的修复发行，不把策划文档当作完成记录。

发版顺序：

1. 在本目录运行 `npm version <版本> --no-git-tag-version`，同步 package 与 lock。
2. 更新 `CHANGELOG.md`，通过规则测试与相关桌面/手机流程。
3. 在仓库根目录运行 `node scripts/build-mc-clicker-2.mjs`，检查正式 HTML 的版本及调试入口隔离。
4. 提交源码、构建产物和本地验证；推送后部署。
5. 服务器备份、资源哈希检查、原子切换 HTML；核对公网资源与普通入口。
6. 发布记录写入 `DEPLOYMENT.md` 及 `docs/qa/`，注明代码提交、备份、验证范围；通过后创建 `mc-clicker-v<版本>` 标签。

同一发行标签不可移动或覆盖。每个已发布版本都应能对应唯一代码提交、构建和备份。
