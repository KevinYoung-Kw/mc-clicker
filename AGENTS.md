# MC Clicker · 每次发布必须遵守

用户确认的长期要求：**每次更新都有更新日志，每次更新都能继续使用旧存档。** 适用于正式版、Alpha 和热修复，不因本批只改 UI、模型或文案而豁免。

- 发布前阅读并完成 [发布要求](docs/RELEASE-CONTRACT.md)。日志或存档验证未通过，不发布。
- 每个游戏发行批次有独立版本号，更新 `docs/CHANGELOG.md`；游戏内 `src/version-history.js` 同步本批玩家可见变化，沿用按大版本归组的展示。发布后记录提交、部署状态和验证结果，不覆盖历史记录。
- 保留旧浏览器存档、存档图、备用存档码及 JSON 的读取能力。升级不能要求玩家重开，不能清空或静默丢弃既有进度。
- 新持久字段必须进入存档／导出链路；可重建数据需要证明能正确恢复。调整数据结构必须提供迁移与历史样本回归，不直接改写已发布的编码字典或默认值。
- 必须验证旧档读取、继续运行、再次保存、刷新恢复和跨浏览器导入。导入失败不覆盖当前世界；成功导入保留恢复备份。测试使用隔离样本，不操作玩家的实际存档。
- 文档／流程自身的更新无需虚增游戏版本；仍须准确记录当前已发布版本，不把未发布实现写成已上线。

## 双版本同步

- 网页版源码在本仓库；小红书版在并列目录 `../mc-clicker-xhs`。不要再改 `my-profile/projects/mc-clicker-2` 作为上游。
- 修改游戏前先读 [双版本同步规则](docs/EDITION-SYNC.md)。公共经营与 UI 修复同批同步网页版和小红书版，逐段保留平台差异，不整仓覆盖。
- 完成后运行 `npm run check:editions`（或 `--peer <另一游戏根目录>`）；同步基线或平台差异变化需重新核查并更新 `docs/edition-sync-state.json`。检查通过不代替游戏、浏览器和离线包验证。
- 小红书性能回移不得降低网页版画质或改变游戏规则。个人网站源码、平台工具 skill、部署凭据和 MC Clicker 3.0 不进入小红书仓库。

## 维护接手入口

- 小修先读 [维护导航](docs/maintenance/README.md) 和 [任务路由](docs/maintenance/TASK-ROUTER.md)，只加载相关专章；不要默认重构大文件。
- 修改前确认真实权威规则、展示消费者、持久化与平台归属。数值调整要验证实际购买/结算结果，不能只替换同名数字。
- 源码或 JS 测试变化后运行 `node scripts/maintenance/scan.mjs` 并以 `--check` 核对索引；不全的隔离副本先补已跟踪依赖，不能放行缺失项。
- 已知未修问题见 [维护清单](docs/maintenance/MAINTENANCE-BACKLOG.md)。反馈已解决则给证据，不重复造一套实现。
- 模型分工按风险：局部文案/样式/明确数值可单独维护；存档、结算时序、前台生命周期、场景缓存的大改先升级审查。不得把练习副本的示例价格并入正式版本。

## UI 更新复查

- 新增或调整界面后，按 [UI 收敛检查](docs/maintenance/UI-REVIEW.md) 复查入口、信息层级、样式复用、局部刷新与手机操作，记录两版证据；设施设置必须能从设施本身进入。
